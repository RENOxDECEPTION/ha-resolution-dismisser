#include <tunables/global>

profile resolution_dismisser flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Allow network access (Supervisor API + HA WebSocket)
  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,

  # S6 overlay init system
  /init rix,
  /package/** rix,
  /command/** rix,
  /etc/s6*/** r,
  /run/** rwk,
  /var/run/** rwk,
  /etc/services.d/** rwk,
  /etc/cont-init.d/** rwk,
  /etc/fix-attrs.d/** rwk,

  # Allow reading add-on data/config
  /data/** rw,

  # Allow executing the main scripts
  /run.sh rix,
  /dismiss_repairs.py rix,

  # System binaries
  /usr/bin/** rix,
  /bin/** rix,
  /usr/sbin/** rix,
  /sbin/** rix,
  /usr/local/bin/** rix,

  # Libraries and Python
  /usr/lib/** rm,
  /lib/** rm,

  # Allow /tmp for Python
  /tmp/** rw,

  # Allow reading system files
  /etc/** r,
  /proc/** r,
  /sys/** r,
  /dev/** rw,
}
